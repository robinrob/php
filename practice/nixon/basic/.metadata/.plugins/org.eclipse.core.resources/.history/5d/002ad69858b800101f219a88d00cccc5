<?php
/* Using the list function. It takes variables for arguments and assigns
 * them to the assigned array in order.
 */

list($a, $b) = array('Alice', 'Bob');
echo 'a = $a, b = $b<br>';

echo "b = $b, c = $c<br>";
?>